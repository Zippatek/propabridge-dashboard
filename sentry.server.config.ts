/**
 * Sentry server-side config placeholder.
 *
 * Activate by installing @sentry/nextjs and uncommenting.
 * See instrumentation.ts for setup steps.
 */

// import * as Sentry from '@sentry/nextjs'

// Sentry.init({
//   dsn: process.env.SENTRY_DSN,
//   environment: process.env.NODE_ENV,
//   tracesSampleRate: 0.1,
// })
